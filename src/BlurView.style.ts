import { StyleSheet } from 'react-native';
import { style } from './utils/styles.utils';


export const styles = StyleSheet.create({
    transparent: style.view({backgroundColor: 'transparent'}),
});
